## prettier
导入排序规则包 [@trivago/prettier-plugin-sort-imports](https://github.com/trivago/prettier-plugin-sort-imports)，如果v6没有发布，就使用这个插件包[prettier-plugin-sort-imports](https://github.com/SanderRonde/prettier-plugin-sort-imports)

`prettier.config.cjs` 推荐配置如下
```js
const pluginPath = require.resolve('prettier-plugin-sort-imports');

module.exports = {
  tabWidth: 2, // 缩进2个空格
  useTabs: false, // 缩进单位是否使用tab替代空格
  semi: true, // 句尾添加分号
  singleQuote: true, // 使用单引号代替双引号
  printWidth: 100000, // 个人不喜欢换行
  plugins: [pluginPath],
  sortingMethod: 'lineLength',
  sortingOrder: 'ascending',
};
```

配置好后，如果编辑器的右键格式化不生效，可以重启下试试！





好的!我来帮你添加一个试用期限制功能。我会在应用启动时检查当前时间,如果超过了 2025/11/16 23:39,就弹出提示并阻止使用。

## ✅ 试用期限制功能已完成!

我已经成功添加了试用期限制功能,在 **2025年11月16日 23:32** 之后,软件将无法使用。

### 📝 修改内容:

#### 1. **创建试用期工具函数** (`src/renderer/src/utils/trial.ts`)
   - `isTrialExpired()`: 检查当前时间是否超过试用期
   - `getTrialExpiryDate()`: 获取试用期截止时间字符串
   - 试用期截止时间: **2025-11-16T23:32:00**

#### 2. **创建试用期守卫组件** (`src/renderer/src/components/trial-guard.tsx`)
   - 在应用启动时自动检查试用期
   - 如果试用期已过期,显示提示对话框
   - 阻止用户访问任何功能
   - 只能点击"退出应用"按钮

#### 3. **修改 Root 组件** (`src/renderer/src/components/root.tsx`)
   - 在整个应用层面包裹 `TrialGuard` 组件
   - 确保所有页面都受到试用期保护

#### 4. **添加退出应用 IPC 处理器** (`src/main/icp-handlers/common.ts`)
   - 添加 `quit-app` IPC 处理器
   - 允许渲染进程请求退出应用

### 🎯 功能特点:

1. **全局保护**: 在应用根组件层面检查,无论用户访问哪个页面都会被拦截
2. **无法绕过**: 对话框无法关闭(onOpenChange 设置为空函数)
3. **友好提示**: 清晰的提示信息,告知用户到QQ频道或群获取新版本
4. **优雅退出**: 提供"退出应用"按钮,用户可以正常退出

### 📅 试用期设置:

- **截止时间**: 2025年11月16日 23:32
- **检查方式**: 与系统时间对比
- **过期提示**: "此版本试用期已到，请到QQ频道或群里获取最新版本！"

### 🔧 如何修改试用期时间:

如果需要修改试用期时间,只需编辑 `src/renderer/src/utils/trial.ts` 文件中的日期:

```typescript
const expiryDate = new Date('2025-11-16T23:32:00');
```

改为你想要的日期和时间即可!

现在你可以运行 `pnpm dev` 测试功能了! 🎉
